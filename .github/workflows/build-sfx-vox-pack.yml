name: Build SFX Vox Documentary Pack

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Test runner
        run: echo "GitHub Actions runner is working"
